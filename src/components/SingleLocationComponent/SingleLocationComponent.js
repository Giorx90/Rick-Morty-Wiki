import React from 'react';
import PropTypes from 'prop-types';
import styles from './SingleLocationComponent.module.scss';

const SingleLocationComponent = () => (
  <div className={styles.SingleLocationComponent}>
    SingleLocationComponent Component
  </div>
);

SingleLocationComponent.propTypes = {};

SingleLocationComponent.defaultProps = {};

export default SingleLocationComponent;
